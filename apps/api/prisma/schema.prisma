generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// Placeholder only — domain models come later
model HealthCheck {
  id        String   @id @default(cuid())
  createdAt DateTime @default(now())
}
